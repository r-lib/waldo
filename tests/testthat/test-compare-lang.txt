> compare(quote(a), quote(b))
x `x` should be `b`, not `a`

> compare(quote(a + b), quote(b + c))
x `x`: /a + b/b + c/

> x <- y <- quote(foo(1:3))
> y[[2]] <- 1:3
> compare(x, y)
x `deparse(x)` equal, but AST non-identical

