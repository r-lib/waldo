> # equal
> f1 <- (function(x = 1, y = 2) { })
> f2 <- (function(x = 1, y = 2) { })
> compare(f1, f2)
v No differences

> # pritimives
> compare(`[`, sum)
x `x` should be `.Primitive("sum")`, not `.Primitive("[")`

> compare(sum, prod)
x `x` should be `.Primitive("prod")`, not `.Primitive("sum")`

> # diff formals
> f3 <- (function(x = 1, y = 1, z = 1) { })
> compare(f1, f3)
x `formals(x)$y`: 2/1
x `formals(x)$z` should be a double vector (1), not absent
x `names(formals(x))`: 'x' 'y' +'z'

> # diff body
> f4 <- (function(x = 1, y = 2) {
+   x + y
+ })
> compare(f1, f4)
x `body(x)`: '{' +'    x + y' '}'

> compare(f1, f4, ignore_srcref = FALSE)
x `body(x)`: '{' +'    x + y' '}'
x `attr(body(x), 'srcref')[[2]]` should be an integer vector (13, 3, 13, 7, 3, ...), not absent
x `attr(body(x), 'wholeSrcref')`: 1 0 2/14 33/1 0 -33 1 2/1 2/14
x `attr(x, 'srcref')`: 2/12 8 2/14 33/1 8 33/1 2/12 2/14

> # diff environment
> environment(f1) <- base_env()
> environment(f2) <- global_env()
> compare(f1, f2)
x `environment(x)` should be <env:global>, not <env:package:base>`

