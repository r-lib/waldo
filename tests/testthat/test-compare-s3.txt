> compare(factor("a"), 1L)
`x` is an S3 object of class <factor>
`y` is an integer vector (1)

> compare(factor("a"), globalenv())
`x` is an S3 object of class <factor>
`y` is an environment

> compare(factor("a"), as.Date("1970-01-02"))
`names(attributes(x))`: "class" "levels"
`names(attributes(y))`: "class"         

`class(x)`: "factor"
`class(y)`: "Date"  

